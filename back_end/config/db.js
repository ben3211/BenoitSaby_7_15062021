/* const mysql = require('mysql');

const db = mysql.createConnection({
   host     : 'localhost',
   user     : 'root',
   password : 'GHRr4Q2c', 
   database : 'groupomania',
   dialect: "mysql",
   pool: {
      max: 5,
      min: 0,
      acquire: 30000,
      idle: 10000
   }
});

module.exports = db; */